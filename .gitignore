# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
api/node_modules
dist
dist-ssr
*.local
.vite

# Env (o padrão .env cobre api/.env em qualquer pasta)
.env
api/.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ferramentas locais do agente (skills instaladas, sessões CLI)
.agents/
.claude/

# Tokens de sessão e dumps de auth (NUNCA committar)
.token
auth.json

# Outputs de teste de geração de PDFs
api/_out/

# Outputs de scripts internos (audits, planos de import, snapshots BD)
# Podem conter PII e dados internos. Sempre prefixados com _ em api/scripts/.
api/scripts/_*.json
